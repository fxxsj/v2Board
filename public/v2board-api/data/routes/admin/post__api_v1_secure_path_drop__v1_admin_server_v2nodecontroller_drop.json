{
  "path": "/api/v1/{secure_path}/server/v2node/drop",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "删除/移除（drop）",
  "params": [
    {
      "name": "id",
      "type": "unknown",
      "required": false,
      "rules": [
        "dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}

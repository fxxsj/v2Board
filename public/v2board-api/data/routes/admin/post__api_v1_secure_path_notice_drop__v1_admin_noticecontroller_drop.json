{
  "path": "/api/v1/{secure_path}/notice/drop",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "删除/移除（notice）",
  "params": [
    {
      "name": "id",
      "type": "unknown",
      "required": true,
      "rules": [
        "required? dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}
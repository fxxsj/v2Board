{
  "path": "/api/v1/{secure_path}/notice/show",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "显示/启用（notice）",
  "params": [
    {
      "name": "id",
      "type": "unknown",
      "required": true,
      "rules": [
        "required? dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}
{
  "path": "/api/v1/{secure_path}/payment/sort",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "排序（payment）",
  "params": [
    {
      "name": "ids",
      "type": "unknown",
      "required": false,
      "rules": [
        "dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}
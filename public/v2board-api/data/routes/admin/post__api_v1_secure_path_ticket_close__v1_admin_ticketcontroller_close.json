{
  "path": "/api/v1/{secure_path}/ticket/close",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "关闭（ticket）",
  "params": [
    {
      "name": "id",
      "type": "unknown",
      "required": true,
      "rules": [
        "required? dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}
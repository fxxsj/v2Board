{
  "path": "/api/v1/{secure_path}/server/trojan/update",
  "method": "POST",
  "auth": "admin(auth_data)",
  "description": "更新（update）",
  "params": [
    {
      "name": "show",
      "type": "string",
      "required": false,
      "rules": [
        "in:0"
      ],
      "source": "FormRequest"
    },
    {
      "name": "id",
      "type": "unknown",
      "required": false,
      "rules": [
        "dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}

{
  "path": "/api/v1/staff/notice/drop",
  "method": "POST",
  "auth": "staff(auth_data)",
  "description": "删除/移除（staff）",
  "params": [
    {
      "name": "id",
      "type": "unknown",
      "required": true,
      "rules": [
        "required? dynamic"
      ],
      "source": "request->input()"
    }
  ],
  "response": {
    "type": "response([..])",
    "keys": [
      "data"
    ],
    "fields": [
      {
        "field": "data",
        "desc": "主返回数据体"
      }
    ]
  }
}